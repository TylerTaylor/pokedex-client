export default (state = [], action) => {
    switch(action.type) {
        case 'GET_CARDS_SUCCESS':
            // let cards = something
            // return cards
        default:
            return state;
    }
}